{
  "name": "pomodoro-app-electronjs",
  "version": "0.0.1",
  "description": "Pomodoro App",
  "main": "build/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc && copyfiles -f src/index.html build",
    "start": "npm run compile && electron"
  },
  "keywords": [
    "Pomodoro"
  ],
  "author": "Fredy M",
  "license": "MIT",
  "devDependencies": {
    "electron": "^32.1.2"
  },
  "dependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^5.6.2"
  }
}
